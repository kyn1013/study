#pragma warning(disable:4996)
#include <iostream>
using namespace std;

int main() {
	int a, b, c;
	scanf("%d %d", &a, &b);
	for (int i = 1; i <= a; i++) {
		scanf("%d", &c);
		if (c < b) {
			printf("%d ", c);
		}
	}

}
